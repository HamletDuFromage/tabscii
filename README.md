# Tabscii

WYSIWYG table editor that generates ASCII and Markdown tables, ready to paste into emails with monospace formatting.

## Run

```bash
docker compose up -d
# visit http://localhost:8080
```

## Develop

Open `public/index.html` in a browser. No build step, no dependencies.
